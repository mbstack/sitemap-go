# sitemap-go
A small Go library that uses Colly to fetch a site's sitemap.xml, parse its URLs, and crawl each linked page to extract additional sitemap links. Lightweight, concurrent, and easy to integrate into other projects.
