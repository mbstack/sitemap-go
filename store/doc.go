// Package store defines the persistence contract for a Scanner
// (the Store interface) and the gorm model types it operates
// on. The default implementation lives in subpackage
// store/sqlite.
package store
